from django.conf.urls import url

from . import views


urlpatterns = [

]
from django.urls import path
from . import views

urlpatterns = [
    # Define las URL aquí utilizando la función path() o re_path()
    # Ejemplo: path('ruta/', views.mi_vista, name='nombre_vista'),
]